[package]
name = "macrocosm"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
quote = { version = "1.0.36", default-features = false }
syn = { version = "1", default-features = false, features = ["full", "extra-traits", "printing"] }
